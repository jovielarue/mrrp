import type { Metadata } from "next";
import { Ubuntu } from "next/font/google";
import "./globals.css";
import UserContextProvider from "./contexts/usercontext";

const ubuntu = Ubuntu({
  weight: "400",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "mrrp",
  description: "A Mega-Robust Rambling Platform generated by Jovie",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <UserContextProvider>
        <body
          className={`${ubuntu.className} antialiased flex flex-col items-center justify-center pt-20 bg-background text-accent min-h-screen w-screen`}
        >
          <div
            className={
              " w-[25rem] max-lg:w-[30rem] max-md:w-[20rem] lg:w-[40rem]"
            }
          >
            {children}
          </div>
        </body>
      </UserContextProvider>
    </html>
  );
}
